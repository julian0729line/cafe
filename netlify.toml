# Configuración para Netlify (https://netlify.com) — alternativa gratuita.
# Netlify detecta Next.js e instala su runtime automáticamente.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20.11.0"
